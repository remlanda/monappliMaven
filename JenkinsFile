// Declarative //
pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				bat 'cd monappliMaven & mvn install'
			}
		 post {
                success {
                    junit 'monappliMaven/target/surefire-reports/*.xml'
                        }
                 }
               
		}
	}
}
