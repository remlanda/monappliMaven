// Declarative //
pipeline {
	agent any
	stages {
		stage('Build') {
			steps {
				bat 'cd Job 8 & mvn install'
			}
		 post {
                success {
                    junit 'Job 8/target/surefire-reports/*.xml'
                        }
                 }
               
		}
	}
}
